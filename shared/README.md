# Shared Types

This directory holds request and response contracts shared by the frontend and backend.

Issue #1 establishes the location with TypeScript protocol types for the React client. Later
issues can add generated or mirrored Rust contracts as API behavior expands.
